<% if @questions %>
  <% @questions.each do |question| %>
    <h3><%= link_to question.title, question_path(question) %>
        by <%= link_to question.user.name, user_path(question.user) %></h3>
    <hr>
  <% end %>
<% end %>
