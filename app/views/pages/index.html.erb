<% if @questions %>
  <% @questions.each do |question| %>
    <p><%= question.text %>by <%= question.user.name %>
      <%= link_to "Answer this question", new_question_answer_path(question) %>
      <br></p>
  <% end %>
<% end %>

<p><%= link_to "Post a Question", new_question_path %></p>
